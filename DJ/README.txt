Download all listed files and folders into a single root directory.
Open the command line/terminal and navigate to root folder.
Run > npm install ejs express node mongodb
Run > node app.js
Navigate to "localhost:4000" on a web browser.